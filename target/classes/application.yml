spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/t_blog?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456


  thymeleaf:
    suffix: .html
    prefix: classpath:/templates/
    cache: true
    mode: HTML
    servlet:
      content-type: text/html
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

comment_avatar: /images/avatar.png
server:
  port: 9999
